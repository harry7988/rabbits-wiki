# 页面设计评价 v1

- 评价对象：http://localhost:5173/（首页 + 急症处置内容页，浅色/深色两种模式）
- 评价日期：2026-08-21
- 评价方式：真实浏览器截图走查（WebBridge）+ 源码核对（`docs/.vitepress/theme/`）
- 结论：视觉走查发现的问题已全部定位到具体代码，美化方案见第四节

## 一、总体评价

**评分：7.5 / 10**

这是一个定位清晰、文案出色的内容型站点（VitePress 技术栈）。信息层级和色彩语义用得相当克制准确：「急症红线」这个概念贯穿首页 hero 卡片和内容页警告框，红色只用于真正的警示，非常专业。深色模式完成度也不错。

主要扣分点：首页目录列表存在明显的布局 bug；六个板块样式雷同、视觉单薄；深色模式有若干细节未适配。

## 二、做得好的地方

1. **信息层级清晰**：hero 区大标题「科学养兔，有据可查」+ 副文案 + 双 CTA（打开工具箱 / 急症速查），主次分明，一眼知道这是什么站、该点哪里。
2. **色彩语义准确**：红色严格用于急症/警示场景（首页红线卡片的左侧红边、"12 小时"高亮、内容页 `.vet-check` 警告框），没有滥用品牌色，建立了可信的视觉语言。
3. **文案即设计**：「停止进食或排便达 12 小时，就是急症」直接放在首屏右侧，内容策略和视觉策略统一，是这个站最聪明的一笔。
4. **深色模式整体可用**：背景、正文、卡片对比度都正常，红色警告框在深色下用了暗红底色（`--rw-danger-soft: #3a1815`），没有刺眼的纯红。
5. **导航克制**：顶部导航 7 个一级入口 + 搜索（⌘K）+ GitHub，数量合适，搜索入口突出，符合文档站心智。
6. **内容页三栏结构标准**：左侧目录 / 正文 / 右侧页内 TOC，符合阅读习惯。
7. **设计 token 体系完整**（源码层面）：`custom.css` 用 CSS 变量组织了全套品牌色（牧草绿 / 干草黄 / 急救红 / 墨绿黑），浅深色双套值，还有左边线三档规格（2/3/4px）和兔点列表符号系统，基础很好。

## 三、问题清单（已定位到代码）

### P0 — 明显 bug

1. **首页目录列表布局错乱**
   - 现象：各板块（急症处置、养兔工具箱、日常养育等）的列表项，圆点贴列最左、文字居中，两者完全分离，像排版事故。
   - 根因：`HomeSections.vue` 中 `.lead-list a` / `.topic-list a` 设置了 `display: flex; justify-content: space-between`，而链接文字是**裸文本节点**，会成为独立的 flex item，被 space-between 推到正中间（圆点 ← 文字 → 透明箭头）。
   - 修复：去掉 `justify-content: space-between`，给 `::after` 箭头加 `margin-left: auto`。

### P1 — 影响质感

2. **首页下半部分单调重复**：六个板块全是「标题 + 一行描述 + 4 条文字链接」的同构排列，无视觉区分，滚动时产生"都是一样的墙"的疲劳感。「急症处置」承载全站红线心智，值得视觉特权位。
3. **深色模式下 logo 露馅**：`docs/public/rabbit-logo.svg` 内含 `#faf6ef` 矩形底色块，深色模式下是一块突兀的白底方块。
4. **深色模式 CTA 按钮荧光绿**：浅色模式主按钮是墨绿近黑（`--rw-ink: #1e2b1a`），深色模式变成亮绿大色块（`--rw-ink: #7ec96a`），与全站暖调色感不协调，在深色背景上过于荧光。
5. **深色模式分隔线对比度过低**：`--rw-line: #2c2c2e`、`--rw-hover-bg: #1f1f21` 在深色背景上几乎不可见，列表失去结构感，hover 反馈缺失。

### P2 — 可以更好

6. **「复制 MD」按钮位置突兀**（`CopyMarkdownButton.vue`）：悬浮在页面右上角、与导航脱节，功能本身很好（面向 AI 工作流），但视觉上像漂浮的异物。建议纳入工具栏体系或弱化样式。
7. **hero 区左右两块间距偏大**：`HomeHero.vue` 网格 `gap: 4rem` + `max-width: 1080px`，在宽屏下标题块与红线卡片之间空白带过大，两者视觉不成组。
8. **「为什么有 rabbits.wiki」板块与上文不统一**（`HomeLayout.vue`）：上面是 1080px 双列网格，这里突然收窄成 720px 单列，宽度跳变明显。绿色引用块（`--rw-grass-soft`）本身合规，保留。
9. **页脚信息重复**：`why-footer` 的「CC BY-SA 4.0 · GitHub 共创 · 关于我们 · 来源索引」与 VitePress 配置页脚的 CC 声明重复出现。

## 四、美化方案 v1（设计意见，已评审）

**设计定位**：信任优先的医学科普百科，编辑部排版语言。Redesign-Preserve 模式——不动信息架构、文案、路由、导航标签，只做视觉精修。克制优先：不加动画特效、不引新依赖、不换字体。

### 方案明细

| # | 对应问题 | 方案 | 涉及文件 |
|---|----------|------|----------|
| 1 | P0-1 | 修 flex bug：`::after { margin-left: auto }` 取代 space-between，恢复「兔点 + 左对齐文字 + 右侧 hover 箭头」 | `HomeSections.vue` |
| 2 | P1-3 | 新增透明底、去文字的 `rabbit-mark.svg`（只留兔子头），放进干草黄软底（`--rw-hay-soft`）圆形徽章，深浅模式都成立，还强化品牌锚点 | `docs/public/rabbit-mark.svg`、`HomeHero.vue` |
| 3 | P1-4 | 深色模式主按钮改为「浅底 + 深字」反转型（保持"墨"的语义，去掉荧光绿） | `custom.css` |
| 4 | P1-2 | 目录区分层：急症处置给红色特权位（标题红 hairline、列表红点、hover 红箭头，复用 `.vet-check` 已有的红点语义）；两个 lead 主题标题上方加 24px 短 hairline 作板块锚点；topic 区保持次级灰 | `HomeSections.vue` |
| 5 | P1-5 | 深色 `--rw-line` → `#38383a`，`--rw-hover-bg` → `#262628` | `custom.css` |
| 6 | P2-7 | hero 收紧：`gap: 4rem → 3rem`，`max-width: 1080px → 1040px` | `HomeHero.vue` |
| 7 | P2-9 | `why-footer` 去掉 CC 协议文字，只留链接（GitHub 共创 / 关于我们 / 全站来源索引），协议声明统一交给页脚 | `HomeLayout.vue` |

### 明确不做

- 不改任何文案、路由、导航标签（SEO + 用户肌肉记忆）。
- 不加动画/滚动特效（医疗科普信任场景，克制优先）。
- 不引入新依赖、新字体、新图标库。
- 不动内容页组件（`.vet-check`、`.rw-alert` 等已定稿，走查无 P0/P1 问题）。

## 五、验收标准

实施后逐项核对（浅色 + 深色各截一张首页）：

- [ ] 目录列表：圆点与文字左对齐排列，无居中文字
- [ ] 深色模式：红线卡 logo 无白底方块，圆形徽章底成立
- [ ] 深色模式：主按钮不再是荧光绿大色块
- [ ] 深色模式：列表分隔线、hover 背景可见
- [ ] 急症板块有红色视觉特权位，与其余板块拉开层级
- [ ] `npm run docs:build` 无错误

## 截图存档

- 浅色首页：`/var/folders/9f/7j58321j6jv1kb3hb2m6qr000000gn/T/kimi-webbridge-screenshots/screenshot_20260822_003717.707.jpg`
- 深色首页：`screenshot_20260822_003806.014.jpg`（同目录）
- 深色内容页（急症处置）：`screenshot_20260822_003822.102.jpg`（同目录）

注：截图为临时目录文件，重启后可能失效，如需长期保存请移入仓库。
